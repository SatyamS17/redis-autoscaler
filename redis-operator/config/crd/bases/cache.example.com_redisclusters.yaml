---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: redisclusters.cache.example.com
spec:
  group: cache.example.com
  names:
    kind: RedisCluster
    listKind: RedisClusterList
    plural: redisclusters
    singular: rediscluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RedisCluster is the Schema for the redisclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RedisClusterSpec defines the desired state of RedisCluster.
            properties:
              autoScaleEnabled:
                type: boolean
              cpuQueryDuration:
                default: 1m
                description: |-
                  CPUQueryDuration is the duration for the anti-jitter CPU average.
                  (e.g., "10m" for stable, "1m" for testing)
                type: string
              cpuQueryWindow:
                default: 1m
                description: CPUQueryWindow is the rate window for the CPU query.
                type: string
              cpuThreshold:
                format: int32
                type: integer
              cpuThresholdLow:
                format: int32
                type: integer
              masters:
                format: int32
                minimum: 1
                type: integer
              memoryHighThreshold:
                default: 80
                description: |-
                  MemoryHighThreshold is the percent of memory usage (vs. request)
                  that triggers a vertical scale-up.
                format: int32
                minimum: 1
                type: integer
              minMasters:
                default: 3
                format: int32
                type: integer
              redisVersion:
                type: string
              replicasPerMaster:
                format: int32
                minimum: 0
                type: integer
              reshardTimeoutSeconds:
                default: 300
                format: int32
                maximum: 3600
                minimum: 60
                type: integer
            required:
            - autoScaleEnabled
            - cpuThreshold
            - masters
            - minMasters
            - replicasPerMaster
            type: object
          status:
            description: RedisClusterStatus defines the observed state of RedisCluster.
            properties:
              avgCpuUsage:
                format: int32
                type: integer
              currentMasters:
                format: int32
                type: integer
              currentReplicas:
                format: int32
                type: integer
              drainDestPod1:
                description: DrainDestPod1 is the first destination pod for drained
                  slots
                type: string
              drainDestPod2:
                description: |-
                  DrainDestPod2 is the second destination pod for drained slots
                  (empty if highest index pod is one of the low-util pods)
                type: string
              initialized:
                type: boolean
              isDraining:
                description: IsDraining locks the autoscaler while a pod is being
                  drained for scale-down.
                type: boolean
              isResharding:
                type: boolean
              lastScaleTime:
                format: date-time
                type: string
              overloadedPod:
                type: string
              podToDrain:
                description: PodToDrain stores the name of the pod selected for draining.
                type: string
            required:
            - avgCpuUsage
            - currentMasters
            - currentReplicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
